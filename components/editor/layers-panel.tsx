"use client"

import LayersPanel from "./panels/layers-panel"

/**
 * @deprecated Use `components/editor/panels/layers-panel` instead.
 * This shim remains to avoid import drift while callers migrate.
 */
export default LayersPanel
