"use client"

import { EnhancedStreamingStudio } from "@/components/streaming/enhanced-streaming-studio"

/** @deprecated Studio UI now uses the canonical enhanced streaming entrypoint. */
export function StreamsDashboard() {
  return <EnhancedStreamingStudio />
}
