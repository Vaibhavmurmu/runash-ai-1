/**
 * @deprecated Compatibility shim for typoed path `payment-getways`.
 * Use `lib/payment-gateways/pay.ts` instead.
 *
 * Removal target: v0.3.0 (or when repository import count reaches zero).
 */
export * from "../payment-gateways/pay"
